# План оптимизации New Avalon: Skirmish

**Дата анализа:** 2026-02-15
**Версия:** 0.2.11
**Статус:** Активный

---

## ✅ Выполнено в v0.2.11 (Stage 2)

### WebRTC P2P Optimization
- ✅ **MessagePack Binary Serialization** - ~72% редукция размера сообщений
  - Добавлен `@msgpack/msgpack` пакет
  - Создан модуль `client/utils/webrtcSerialization.ts` (796 строк)
  - Оптимизированная сериализация карт (id + stats, токены отдельно)
  - Short property keys для delta формата

### Code Splitting
- ✅ **useGameState.ts рефакторинг** - разбит на 18 фокусированных модулей:
  - `useGameLifecycle.ts` - создание/присоединение/выход из игры
  - `gameCreators.ts` - генерация gameId, создание колоды, начальное состояние
  - `gameStateStorage.ts` - сохранение/загрузка/очистка состояния
  - `useVisualEffects.ts` - визуальные эффекты (highlights, floating texts)
  - `useWebRTC.ts` - управление WebRTC P2P соединениями
  - `useTargetingMode.ts` - универсальная система таргетинга
  - `useBoardManipulation.ts` - операции с доской
  - `useCardMovement.ts` - drag-and-drop карт
  - `useCardOperations.ts` - способности и статусы карт
  - `useCardStatus.ts` - управление статусами карт
  - `useDeckManagement.ts` - операции с колодой
  - `useGameSettings.ts` - конфигурация игры
  - `usePhaseManagement.ts` - переходы фаз
  - `usePlayerActions.ts` - действия игроков
  - `useReadyCheck.ts` - система готовности
  - `useScoring.ts` - подсчёт очков
  - `websocketHelpers.ts` - WebSocket утилиты
  - `types.ts` - общие типы

### Bug Fixes
- ✅ **DeckType Runtime Error** - исправлен import type vs regular import
- ✅ **WebRTC Reconnection Loop** - исправлен бесконечный цикл при HMR
  - Добавлен sessionStorage флаг для предотвращения дублирования auto-restore
- ✅ **Unused Imports** - удалены неиспользуемые импорты

### Результат
- **useGameState.ts**: 6,884 → 3,775 строк (-45%)
- **WebRTC сообщения**: ~72% редукция размера
- **Код**: более модульный и поддерживаемый

---

## ✅ Выполнено в v0.2.11+ (Stage 3)

### Unified Modal System

**Проблема:** 13+ модальных компонентов с дублирующимся кодом, отсутствием унифицированного управления состоянием и отсутствием code splitting.

**Решение:**

1. **React Context-based Modal Store** (`client/hooks/useModals.tsx`)
   - Создан централизованный store для управления всеми модалками
   - Заменил Zustand (проблемы с v5) на React Context
   - Единственный источник правды для состояния модалок

2. **ModalsRenderer** (`client/components/ModalsRenderer.tsx`)
   - Централизованный компонент для рендеринга всех модалок
   - Поддержка React.lazy для code splitting
   - Единая точка рендеринга в App.tsx

3. **Code Splitting для модалок**
   - SettingsModal: 6.12 KB
   - DeckBuilderModal: 13.45 KB
   - RulesModal: 24.52 KB
   - JoinGameModal: 3.37 KB
   - И другие игровые модалки...

4. **Refactored App.tsx**
   - Разделение на `AppInner` и `App` компоненты
   - `ModalsProvider` обёртывает всё приложение
   - `ModalsRenderer` добавлен в оба return (MainMenu и Game)

**Технические детали:**
```typescript
// useModals.tsx - React Context implementation
export const ModalsProvider = ({ children }: { children: ReactNode }) => {
  const [openModal, setOpenModal] = useState<ModalType | null>(null)
  const [modalData, setModalData] = useState<Record<string, any>>({})
  // ...
}

// ModalsRenderer.tsx - Unified rendering
export const ModalsRenderer = () => {
  const { openModal, close, getData, getSize } = useModals()
  // Ленивая загрузка и условный рендеринг модалок
}
```

**Проблемы и решения:**
- ❌ Zustand v5 не работал правильно - перешли на React Context
- ❌ ModalsRenderer был только в игровом состоянии - добавлен и в MainMenu
- ❌ Двойная обёртка (BaseModal + собственная обёртка) - удалена для правил/настроек

**Результат:**
- **Модальные окна работают корректно** - Settings, Rules, Deck Builder, Join Game
- **Code splitting** - модалки загружаются по требованию
- **Единый API** - `useSettingsModal()`, `useRulesModal()`, и т.д.
- **Уменьшенный основной bundle**

---

## 📊 TODO - Что осталось сделать

### 1. PlayerPanel.tsx - 1,157 строк 🔴 Критический

**Проблема:** Компонент содержит слишком много логики

**Предлагаемая структура:**
```
PlayerPanel.tsx (контейнер, ~200 строк)
├── PlayerHeader.tsx           # Имя, цвет, очки (~100 строк)
├── PlayerHand.tsx             # Карточные руки (~300 строк)
├── PlayerDeckControls.tsx     # Управление колодой (~200 строк)
├── PlayerDiscard.tsx          # Отображение discard (~150 строк)
├── PlayerAnnounced.tsx        # Отображение announced (~100 строк)
└── PlayerStatus.tsx           # Статус готовности (~100 строк)
```

### 2. Улучшить memoization 🟡 Высокий

**Проблема:** Лишние рендеры компонентов

**Решение:**
- Улучшить memo в PlayerPanel
- Добавить useTransition для тяжёлых операций
- Использовать useDeferredValue для визуальных эффектов

### 3. Батчинг визуальных эффектов 🟢 Средний

**Проблема:** Эффекты отправляются по одному

**Решение:** Собирать эффекты в батч и отправлять раз в 50мс

```typescript
const EFFECT_BATCH_INTERVAL = 50
// Отправлять батч эффектов вместо одиночных сообщений
```

### 4. Virtual Scrolling 🟢 Средний

**Проблема:** Большие списки карт рендерятся целиком

**Решение:** Использовать react-window для hand/deck

### 5. Оптимизация ресурсов 🟢 Низкий

**Идеи:**
- Разделить contentDatabase.json по фракциям
- Lazy loading для изображений карт
- Progressive image loading

---

## 📋 Текущее состояние сборки

```
index.html              2.72 KB │ gzip: 0.89 KB
CSS                     50.99 KB │ gzip: 9.16 KB
vendor-webrtc          38.65 KB │ gzip: 11.34 KB (PeerJS)
vendor                 76.07 KB │ gzip: 20.74 KB (зависимости)
vendor-react          137.55 KB │ gzip: 43.99 KB (React + DOM)
game-logic            259.67 KB │ gzip: 63.21 KB (хуки + shared)
index (main)           240-270 KB │ gzip: ~75 KB (после code splitting модалок)
Ленивые модалки:        ~50 KB (грузятся по требованию)
```

**Итого:** ~770-800 KB (без gzip), ~215 KB (с gzip)

**Улучшения:**
- ✅ Code splitting для 4+ меню модалок (~47 KB вынесено в lazy chunks)
- ✅ Унифицированная система управления модалками
- ✅ Меньше дубликации кода

---

## 🎯 Приоритеты на следующую версию

1. 🔴 **PlayerPanel.tsx рефакторинг** - критично для поддерживаемости
2. 🟡 **Улучшить memoization** - улучшение производительности
3. 🟢 **Батчинг эффектов** - оптимизация трафика
4. 🟢 **Virtual Scrolling** - для больших списков карт

---

## 📚 Полезные ресурсы

- [React Performance](https://react.dev/learn/render-and-commit)
- [Vite Performance](https://vitejs.dev/guide/performance.html)
- [WebRTC Optimization](https://webrtc.org/getting-started/performance)
- [Bundle Analysis](https://bundlephobia.com/)

---

**Автор анализа:** Claude (Anthropic)
**Последнее обновление:** 2026-02-16
